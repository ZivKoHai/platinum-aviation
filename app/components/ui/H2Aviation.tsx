export function H2Aviation({ children }: { children: React.ReactNode }) {
  return (
    <h2 className="text-2xl sm:text-4xl font-medium text-black text-center capitalize">
      {children}
    </h2>
  );
}
